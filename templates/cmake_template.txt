cmake_minimum_required(VERSION 2.6)

include_directories("MrT/Modules")
include_directories("src")
include_directories("src/lib")

add_subdirectory(MrT/Modules/Utilities/PolyPacket)

add_definitions(
    -g
    -DPOLY_PACKET_DEBUG_LVL=1
    )

add_library(
  ${proto.fileName}
  src/lib/${proto.fileName}.c
  src/lib/linux_uart.c
  )


add_executable(
  ${proto.fileName}_util
  src/main.c
  src/app_${proto.name.lower()}.c
  )

target_link_libraries(${proto.fileName} PolyPacket)

target_link_libraries(${proto.fileName}_util ${proto.fileName} )
